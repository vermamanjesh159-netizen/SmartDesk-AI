name: Keep Alive

on:
  schedule:
    - cron: '*/30 * * * *'  # runs every 30 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl https://smartdesk-ai-fbs9vawbzhw4qmjb4ek9fo.streamlit.app/
